- We should evaluate if we could just use Google Maps. Wouldthat make things much easier?
- Use gemma4:e2b + web search for getting metadata. Not that important for the MVP.
- We need GitHub Actions for automated tests
- The project should have tests:
  - Check that it builds
  - go vet
  - Syntax check JS
  - Compare i18n keys (Keys should exist in all languages)
  - Playwright UI tests (use Firefox)
- Check navigation forward/backward in browser. This seems to not always work
- Esay way to start development mode with some test data
- No one runs this yet: Collapse all migrations. We should do this before the first release
- Markdown for longer texts. This doesn't seem to work properly right now
- Linked images should actually be downloaded and served locally similar to uploaded images
- Breadcrum navigation
- New location editor should allow setting all the things the edit location allows
- We should deliver documents the browser can display with the correct mime type so that the browser e.g. displays PDFs instead of offering it for download
- Trips should have checklists (title + list items) which I can check off in preparation for a trip
